import { APP_TITLE } from '../../data';
import './Header.scss';

const Header = () => {
    return (
        <header>
            <h1>{APP_TITLE}</h1>
        </header>
    );
};

export default Header;
